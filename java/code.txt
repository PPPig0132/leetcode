package Tree;
// letcode  257
//给你一个二叉树的根节点 root ，按 任意顺序 ，返回所有从根节点到叶子节点的路径。
//叶子节点 是指没有子节点的节点。


import java.util.*;

public class BinaryTerrPaths {
    public List<String> binaryTreePaths(TreeNode root) {
        List<String> res=new ArrayList<>();
        backtrack(root,res,new StringBuilder());
        return res;
        
    }

    public backtrack(TreeNode root, List<String> ans, StringBuilder cur){
        cur.append(root.val);
        if(root.left==null && root.right==null){
            res.add(sb.toString());
            return;
        }
        cur.append("->");
        if(root.left!=null){
            backtrack(root.left,ans,cur);
        }
        if(root.right!=null){
            backtrack(root.right,ans,cur);
        }
        cur.remove(cur.size-3);
    }
}
